node_modules/
dist/

# Packaged executables and the SEA intermediates that produce them.
build/

# Capacitor regenerates the native Android project from capacitor.config.json
# and the built web assets, so it is an artifact rather than source.
apps/mobile/android/
apps/mobile/ios/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Captured price files are the user's own data, not source. The format is
# documented in imports/README.md and imports/**/example.csv.template.
imports/**/*.csv

# Local quote log / paper-trade database. Regenerated by the poller.
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.cache/
